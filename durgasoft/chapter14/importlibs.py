print(""" we can use import to import lib/modules, below are the different types to import the module """)

import math

print(math.pi)

from math import sqrt

print(sqrt(9))

import math as m

print(m.pow(10,2))